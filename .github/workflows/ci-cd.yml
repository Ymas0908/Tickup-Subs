name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    services:
      mariadb:
        image: mariadb:10.11
        env:
          MARIADB_ROOT_PASSWORD: root
          MARIADB_DATABASE: tickupDB
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h localhost"
          --health-interval=10s
          --health-timeout=60s
          --health-retries=10
          --entrypoint="docker-entrypoint.sh --sql-mode=STRICT_TRANS_TABLES"

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Cache Maven dependencies
      uses: actions/cache@v3
      with:
        path: ~/.m2
        key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
        restore-keys: ${{ runner.os }}-m2

    - name: Install Poseidon themes
      run: |
        mvn install:install-file \
          -Dfile=poseidon-theme-6.0.0-jakarta.jar \
          -DgroupId=org.primefaces.themes \
          -DartifactId=poseidon-jakarta \
          -Dversion=6.0.0 \
          -Dpackaging=jar
        mvn install:install-file \
          -Dfile=poseidon-theme-6.0.0.jar \
          -DgroupId=org.primefaces.themes \
          -DartifactId=poseidon \
          -Dversion=6.0.0 \
          -Dpackaging=jar

    - name: Wait for MariaDB to be ready
      run: |
        echo "Waiting for MariaDB to be ready..."
        echo "Checking MariaDB status..."
        docker ps
        
        for i in {1..60}; do
          echo "Attempt $i/60: Checking MariaDB..."
          if mysqladmin ping -h"127.0.0.1" -P3306 -uroot -proot --silent; then
            echo "MariaDB is ready!"
            echo "Creating test connection..."
            mysql -h"127.0.0.1" -P3306 -uroot -proot -e "SELECT 1"
            break
          fi
          echo "MariaDB not ready yet, waiting 2 seconds..."
          sleep 2
        done
        
        echo "Final MariaDB status:"
        docker ps

    - name: Run tests
      run: mvn clean test
      env:
        SPRING_DATASOURCE_URL: jdbc:mariadb://localhost:3306/tickupDB
        SPRING_DATASOURCE_USERNAME: root
        SPRING_DATASOURCE_PASSWORD: root

  build-and-deploy:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: |
          ${{ secrets.DOCKER_USERNAME }}/tickup-subs:latest
          ${{ secrets.DOCKER_USERNAME }}/tickup-subs:${{ github.sha }}
        cache-from: type=gha
        cache-to: type=gha,mode=max

  deploy:
    needs: build-and-deploy
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: Deploy to production
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.SSH_KEY }}
        script: |
          cd /opt/tickup-subs
          docker-compose down
          docker pull ${{ secrets.DOCKER_USERNAME }}/tickup-subs:latest
          docker-compose up -d
          docker system prune -f
